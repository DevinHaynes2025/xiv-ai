import { CommerceCsvDemoScreen } from '@/screens/os';

/** US-SYS-01 — Business role can preview the same CSV/commerce demo stub. */
export default function BusinessCommerce() {
  return <CommerceCsvDemoScreen />;
}
