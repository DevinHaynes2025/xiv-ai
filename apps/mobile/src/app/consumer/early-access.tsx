import ConsumerInnovateScreen from './innovate';

/** Early-access route — US-CON-01 consumer innovate / early access loop. */
export default function EarlyAccess() {
  return <ConsumerInnovateScreen />;
}
