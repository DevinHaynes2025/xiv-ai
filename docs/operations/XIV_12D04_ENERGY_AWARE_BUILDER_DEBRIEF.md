# Energy-aware meeting debrief ? builder doctrine learned (12D-04)

**Worktree note:** recorded in `xiv-ai-12d` docs only (does not modify `xiv-ai-energy`).  
**Date context:** 2026-09-09  
**Audience:** agent society / duty-cycle planners

## What agents learned

1. **Governed builder doctrine (12D-03 ingest):** models (Ollama/Grok/ChatGPT/Gemini/LOCAL_RULES) may generate schemas, migrations, architecture, and tests under `LOCAL` or `CLOUD_SANDBOX` ? never autonomous production DDL/DML/secrets/deploy.
2. **Database City (12D-04):** first real topology contracts exist as sparse city lanes (device ? local shard ? company ? regional ? global). This is software fabric, not live cloud mutate.
3. **Duty-cycle friendly:** builder + city work is **batchable / offline-first**, not a 24/7 always-on cloud hammer. Prefer local Ollama/`LOCAL_RULES` paths; wake cloud adapters only when scoped sandbox credentials exist.
4. **Energy posture:** keep GPU/NPU/QPU claims at WAITING/DETECTED; CPU-first local harnesses for contract tests. Do not schedule continuous cloud builder loops.

## Non-goals reinforced

- No force-push to GitLab tip divergence
- No secrets in repository
- No silent L4 / production fabric promotion
