# CEO checkpoint — 12D-95 authenticated presence

For Devin Xavier Haynes, CEO & Cofounder of XIV AI.

## Newly received evidence

The uploaded Windows transcript reports the 12D-92 local Qwen meeting completing at 2026-09-12T00:44:25Z (September 11, 7:44:25 PM in Dallas). Its packet is AWAITING_REVIEW, remoteCallsMade=0, learningPromoted=false and humanDecision=REQUIRED. This supersedes the earlier blocked-at-classifier checkpoint only for that reported local invocation. It is not a current remote probe, a signed worker-presence report or proof of a three-model meeting. The external CLI reports glm-5.3-flash:cloud. Grok remains pending.

The pasted server-2.log startup from September 10 reports ROCm rejecting gfx1103 and Vulkan detecting but skipping the integrated Radeon 780M. Later model output reports successful CPU inference. Leave the working pilot configuration unchanged. The historical 22.1 GiB available-memory line is not a current measurement. One-request-at-a-time is our conservative pilot policy, not a proven one-model hardware maximum.

## This implementation

Imported the previous local 12D-95 collector into a new private review branch based on the inspected 12D-94 bounded-shift source. Added terminal STOPPED semantics and explicit non-attestation flags. Added a report adapter, inventory-only CLI, integration test using real registry imports, and narrow exports. Both existing blocking CI jobs remain blocking; no existing source census, scheduler, model router or host worker is replaced.

The collector validates signature, tenant/instance/provider/model/document/source binding, expiry and monotonic sequence. Replay state belongs to the collector. Forged high sequences do not consume it. Old collector-session messages cannot be reused after restart. Operational private keys are not generated, read or published.

Local validation: Node 22.16.0 / TypeScript 5.8.3, scoped strict noEmit check succeeded and 34 protocol/report unit tests passed (zero failed/skipped). These are synthetic payloads signed with temporary test keys. The actual-registry integration test and full-service typecheck are delegated to branch CI; read final-head job traces before claiming those passed. Test fixtures are not live workforce evidence.

## Limits and next queue

No fresh Claude Code or Grok review of this patch has been received. No Windows code, GPU settings, model weights, cloud resource or production service was changed. No merge was performed. No live instance is newly enrolled. The current whole-fleet live count remains unknown.

Next: independently review this branch, then build one supervised host reporter with reviewed key custody and a shared local job lease. Do not add another independent daemon or bypass the unresolved Homebase/12D scheduling reconciliation. Preserve tenant isolation and least privilege; no security gate is deferred based on the Qwen memo.
