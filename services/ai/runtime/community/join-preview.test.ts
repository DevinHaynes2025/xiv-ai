import assert from'node:assert/strict';import{previewCommunityJoin}from'./join-preview';
const p=previewCommunityJoin({userId:'u1',communityId:'supply-chain'});assert.equal(p.status,'REVIEW_REQUIRED');assert.equal(p.membershipCreated,false);assert.equal(p.accountsConnected,false);assert.equal(p.devicesControlled,false);assert.equal(p.earningsGuaranteed,false);assert.equal(p.partnerStatus,'NOT_CONFIGURED');assert.throws(()=>previewCommunityJoin({userId:'',communityId:'valid'}));assert.throws(()=>previewCommunityJoin({userId:'u1',communityId:'../other'}));console.log('Community join preview contracts: OK');
