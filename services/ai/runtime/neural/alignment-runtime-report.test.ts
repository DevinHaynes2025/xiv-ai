import assert from'node:assert/strict';import{buildRuntimeAlignmentReport,DISCONNECTED_ALIGNMENT_RUNTIME,type AlignmentRuntimeSource}from'./alignment-runtime-report';
const disconnected=buildRuntimeAlignmentReport();assert.equal(DISCONNECTED_ALIGNMENT_RUNTIME.kind,'DISCONNECTED');assert.equal(disconnected.assurance.trustStore,'NOT_CONFIGURED');assert.equal(disconnected.assurance.auditChain,'NOT_CONNECTED');assert.equal(disconnected.assurance.productionActivation,'DISABLED');const broken:AlignmentRuntimeSource={kind:'LOCAL_REFERENCE',read:()=>{throw new Error('sensitive storage failure')}};const failed=buildRuntimeAlignmentReport(broken);assert.equal(failed.assurance.trustStore,'NOT_CONFIGURED');assert.equal(JSON.stringify(failed).includes('sensitive storage failure'),false);const partial=buildRuntimeAlignmentReport({kind:'LOCAL_REFERENCE',read:()=>({evidence:[],liveTrustAudit:{tenantId:'t1',universeId:'u1',storeVersion:1,eventCount:1,chainHead:'a'.repeat(64),auditComplete:true}})});assert.equal(partial.assurance.trustStore,'LOCAL_REFERENCE');assert.equal(partial.assurance.alignmentEvidenceUsable,false);assert.equal(partial.summary.productionLiveCount,0);console.log('Alignment runtime report contracts: OK');
