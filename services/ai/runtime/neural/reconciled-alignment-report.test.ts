import assert from'node:assert/strict';import{buildReconciledAlignmentReport}from'./reconciled-alignment-report';
const live={tenantId:'t1',universeId:'u1',storeVersion:4,eventCount:4,chainHead:'a'.repeat(64),auditComplete:true},checkpoint={tenantId:'t1',universeId:'u1',configured:true,storeVersion:4,eventCount:4,chainHead:'a'.repeat(64)};const disconnected=buildReconciledAlignmentReport({evidence:[]});assert.equal(disconnected.assurance.trustStore,'NOT_CONFIGURED');assert.equal(disconnected.assurance.productionActivation,'DISABLED');const partial=buildReconciledAlignmentReport({evidence:[],liveTrustAudit:live});assert.equal(partial.assurance.trustStore,'LOCAL_REFERENCE');assert.equal(partial.assurance.auditChain,'NOT_CONNECTED');assert.equal(partial.assurance.alignmentEvidenceUsable,false);const verified=buildReconciledAlignmentReport({evidence:[],liveTrustAudit:live,acceptedCheckpoint:checkpoint});assert.equal(verified.assurance.auditChain,'VERIFIED_CHECKPOINT');assert.equal(verified.assurance.alignmentEvidenceUsable,true);const quarantined=buildReconciledAlignmentReport({evidence:[],liveTrustAudit:{...live,chainHead:'b'.repeat(64)},acceptedCheckpoint:checkpoint});assert.equal(quarantined.assurance.trustStore,'QUARANTINED');assert.equal(quarantined.assurance.auditChain,'INVALID');assert.equal(quarantined.assurance.alignmentEvidenceUsable,false);assert.equal(quarantined.summary.productionLiveCount,0);console.log('Reconciled alignment report contracts: OK');
