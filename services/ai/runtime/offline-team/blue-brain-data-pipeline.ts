import { createHash } from 'node:crypto';
export type PipelineClass='PUBLIC'|'INTERNAL'|'CONFIDENTIAL'|'TOP_SECRET';
export interface AtomicPipelineCell{id:string;tenantId:string;contentHash:string;provenanceRefs:string[];classification:PipelineClass;bytes:number;}
export interface BlueBrainScaleReceipt{measuredCells:number;measuredBytes:number;targetTier:'LOCAL_MVP'|'MILLION_SCALE'|'BILLION_SCALE'|'TRILLION_SCALE_TARGET';virtualChipModel:'LOGICAL_ATOMIC_CELL';quantumExecution:'CLASSICAL'|'QUANTUM_SIMULATOR'|'QPU_VERIFIED';}
export function createAtomicPipelineCell(i:{tenantId:string;content:string;provenanceRefs:string[];classification:PipelineClass}):AtomicPipelineCell{if(!i.tenantId.trim())throw new Error('tenant required');if(!i.provenanceRefs.length)throw new Error('provenance required');const h=createHash('sha256').update(i.content).digest('hex');return{id:createHash('sha256').update(`${i.tenantId}:${h}:${i.classification}`).digest('hex'),tenantId:i.tenantId,contentHash:h,provenanceRefs:[...new Set(i.provenanceRefs)],classification:i.classification,bytes:Buffer.byteLength(i.content)}}
export function buildScaleReceipt(c:AtomicPipelineCell[],t:BlueBrainScaleReceipt['targetTier']):BlueBrainScaleReceipt{return{measuredCells:c.length,measuredBytes:c.reduce((s,x)=>s+x.bytes,0),targetTier:t,virtualChipModel:'LOGICAL_ATOMIC_CELL',quantumExecution:'CLASSICAL'}}
export const BLUE_BRAIN_PIPELINE_GUARDRAILS= Object.freeze({ humanDecision: 'REQUIRED' as const, targetIsMeasuredCapacity:false,atomSizedPhysicalStorageClaim:false,qpuAdvantageClaimWithoutEvidence:false,crossTenantPoolingAllowed:false,provenanceRequired:true} );
