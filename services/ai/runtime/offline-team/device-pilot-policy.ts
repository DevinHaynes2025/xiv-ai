import type { DevicePilotCapability, DevicePilotEnrollment } from './device-pilot-enrollment';
export type DevicePilotActionRisk='LOW'|'MEDIUM'|'HIGH'|'IRREVERSIBLE';
export interface DevicePilotAction{actionId:string;tenantId:string;deviceId:string;capability:DevicePilotCapability;risk:DevicePilotActionRisk;requiresNetwork:boolean;containsTopSecret:boolean;}
export interface DevicePilotDecision{allowed:boolean;requiresHumanApproval:boolean;reason:string;}
export function authorizeDevicePilotAction(e:DevicePilotEnrollment,a:DevicePilotAction):DevicePilotDecision{if(e.tenantId!==a.tenantId||e.deviceId!==a.deviceId)return{allowed:false,requiresHumanApproval:true,reason:'tenant or device mismatch'};if(!e.capabilities.includes(a.capability))return{allowed:false,requiresHumanApproval:true,reason:'capability not consented'};if(a.containsTopSecret&&a.requiresNetwork)return{allowed:false,requiresHumanApproval:true,reason:'TOP_SECRET cannot leave the governed local boundary'};const gated=a.risk==='HIGH'||a.risk==='IRREVERSIBLE';return{allowed:true,requiresHumanApproval:gated,reason:gated?'high-impact action requires explicit approval':'within enrolled capability and policy'};}
