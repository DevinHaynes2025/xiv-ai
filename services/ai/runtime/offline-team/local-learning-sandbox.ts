import { createHash } from 'node:crypto';
export interface LocalLearningExample{tenantId:string;sourceRef:string;content:string;approved:boolean;classification:'PUBLIC'|'INTERNAL'|'CONFIDENTIAL'|'TOP_SECRET';}
export interface LocalLearningReceipt{exampleId:string;tenantId:string;sourceRef:string;classification:LocalLearningExample['classification'];contentHash:string;mode:'RAG_MEMORY_EVAL'|'FINE_TUNE_CANDIDATE';modelWeightsMutated:false;}
export const LOCAL_LEARNING_GUARDRAILS= Object.freeze({ humanDecision: 'REQUIRED' as const, approvalRequired:true,provenanceRequired:true,productionSelfModificationAllowed:false,modelWeightMutationByDefault:false,topSecretCloudTrainingAllowed:false,rollbackRequiredForFutureFineTuning:true} );
export function stageLocalLearning(e:LocalLearningExample):LocalLearningReceipt{if(!e.approved)throw new Error('learning example requires explicit approval');if(!e.sourceRef)throw new Error('learning example requires provenance');const contentHash=createHash('sha256').update(e.content).digest('hex');return{exampleId:`learn-${contentHash.slice(0,16)}`,tenantId:e.tenantId,sourceRef:e.sourceRef,classification:e.classification,contentHash,mode:'RAG_MEMORY_EVAL',modelWeightsMutated:false};}
